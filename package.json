{
  "name": "erania-web-vercel",
  "version": "1.0.0",
  "description": "ERANIA Web App for Vercel deployment",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build needed'",
    "start": "node server.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}